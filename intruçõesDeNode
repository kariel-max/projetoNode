# Para criar um package.json no terminal digite ''npm init'' ou ''npm init -y''

# // para exibir no node os conteudo que vc quer basta por ''node e o nome da pasta'' ex.: ''node index.js''

# //para criar o tsconfing.ts no terminal ponhar ''tsc --init''

# // tsconfig.json muder o target para es6, pois é o mais compativel atualmente, abaixo dele você pode por ''"module":"commonjs"'', para indetificar a linguangens que você esta usado no momento, depois ponha '' "moduleResolution": "node" '', é o servido que você ira usar

# // rootDir: serve para dizer onde sera destinado os codigo que você desenvolveu. ex.: "rootDir": "./src"

# // outDir: serve para dizer onde vai começar o conteldo para depois ser tranferido para o enderenço do rootDir. ex.: "outDir": ./dist"

# // para ter uma melhor experiencia no codigo user '' npm install --save-dev @types/node '' após só espere ele intalar a biblilhotecar da linguangen

# // watch mode: user no teminal tsc -w para usar, após isso crier outro terminal para roda o node.

# // script no package.json abrar o package.json vá em script e adicioner '' "start" '': node mais pasta que você quer que ele roder. ex.: "start": "node dist/index.js". após isso vá para o terminal e ponha 'npm rum start' para ele executa o comando de start.

# rotas

# para exibir algum no seu servido use "get" que significa que ele ira pegar conteudo de algum lugar

# é nesseçario criar rotas de acesso de conteudo vc pode criar usando no imports junto ao {Routes, Request, Response}

# para criar um imports : use ''import "mais o nome que vc que", { Request, Response} from 'mais a biblilhoteca'; '' ex.: import express, { Request, Response} from 'express';

# use uma const para amazenar a biblilhoteca ex.: const server = exepress();

# após de armazernar a biblilhoteca crier um metódo para exibir o seu conteudo ex.: server.get('/', (req: Request, res: Response)=> { res.send('Óla mundo'); });

# após isso crier um porta para que o seu servido seja encotrado ex.: sever.listen(3000)

# !! se vc fez tudo isso e o seu servido não foi encontrado verifique se o seu watch-mode esta ligado ou algum do tipo !!

# já no seu servido para criar novas rotas, após de cada / pode um novo endereço ex.: server.use('/contato' ()=> { digite algum });

# sendo que primeiro vc tem que criar um imports com essa rota. ex.: imports mainRoutes from './routes/index'; ./routes/index só mostra que esta sendo pegor de uma pasta que esta dentro de outra;

# sendo que nessa pasta estão sendo exportadas as rotas!!!!!
 
# no node vc pode exporta varios conteudos ou um só, para exporta conteudos se for todos de uma vez só use 'export default mais o nome que vc escolheu na const que armazena o imports'

# sempre crier conteudo em pastas semparadas afim de organizar o conteudo



